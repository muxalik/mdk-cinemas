export enum Variants {
  primary = 'primary',
  secondary = 'secondary',
  warning = 'warning',
  success = 'success',
  info = 'info',
  danger = 'danger',
}

export enum CinemaStatuses {
  opened = 'Opened',
  closed = 'Closed',
  any = 'Any',
}

export enum MovieStatuses {
  available = 'Available',
  not_available = 'Not available',
  any = 'Any',
}
